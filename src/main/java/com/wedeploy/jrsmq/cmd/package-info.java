/**
 * Commands implementations. Each command encapsulate one business method.
 * Infrastructure is set via constructor.
 * Command methods are fluent, human-readable methods for collecting
 * all arguments and data required for execution. Prior to any execution,
 * validation is required.
 */
package com.wedeploy.jrsmq.cmd;